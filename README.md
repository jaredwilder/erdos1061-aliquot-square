# Erdős #1061 — sigma / aliquot-square program

**Author:** Jared Wilder  
**Status:** infinite-family, primitive-seed, ray-scaling, exact-certificate, and lower-bound program; no blanket claim that every aspect of the parent problem is closed.

This repository is the canonical public home for the estate's #1061 mathematics. The program goes well beyond the compact `(a,2a)` family: it includes a primitive-seed generator for aliquot-square solutions, primitive-ray scaling, exact asymptotic contribution formulas, a large seed certificate bank and verifier, a second exact search, released lower-bound coefficient work, and Mersenne-power specializations.

## Evidence lanes

The repository keeps theorem statements, exact finite searches, certificate-backed counts, lower-bound derivations, and source-recovery obligations distinct. In particular, the historically referenced **152,803-seed** certificate is a computational artifact, not an automatic asymptotic theorem.

## Source layout

Exact public source bytes are migrated under:

- `sigma-family/` — main #1061 extraction;
- `rays/` — primitive-ray and asymptotic work;
- `ore-findings/` — released #1061 findings from the ore audit.

Large certificate-byte recovery remains a separate provenance obligation where the original bytes are not yet in a canonical public tree.
